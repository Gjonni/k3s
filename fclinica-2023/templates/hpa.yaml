{{- if .Values.fclinica.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "fclinica-2023.fullname" . }}
  labels:
    {{- include "fclinica-2023.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "fclinica-2023.fullname" . }}
  minReplicas: {{ .Values.fclinica.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.fclinica.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.fclinica.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.fclinica.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.fclinica.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.fclinica.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
