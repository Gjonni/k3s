apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fclinica-2023.fullname" . }}-test-connection"
  labels:
    {{- include "fclinica-2023.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fclinica-2023.fullname" . }}:{{ .Values.fclinica.service.port }}']
  restartPolicy: Never
